<%= form_with(model: recipe_tag) do |form| %>
  <% if recipe_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_tag.errors.count, "error") %> prohibited this recipe_tag from being saved:</h2>

      <ul>
        <% recipe_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :recipe_id, style: "display: block" %>
    <%= form.text_field :recipe_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.text_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
